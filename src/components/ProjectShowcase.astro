---
import { Image } from "astro:assets";

import type { Project } from "../data/projects";

interface Props {
  project: Project;
}

const { project } = Astro.props;

const mediaLink = project.videoUrl ?? project.demoUrl;
const previewImageClass = "h-full w-full object-cover opacity-80";
---

<article class="overflow-hidden rounded-xl bg-off-white/50 ring-1 ring-sepia/25 dark:bg-white/30 dark:ring-dim-muted">
  <div class="flex flex-col gap-4">
    <div class="order-first aspect-[5/2] w-full overflow-hidden">
      {
        mediaLink ? (
          <a
            href={mediaLink}
            target="_blank"
            rel="noopener noreferrer"
            class="block h-full overflow-hidden focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-sepia"
            aria-label={`${project.name} demo preview`}
          >
            <Image
              src={project.previewImage}
              alt={project.previewAlt}
              widths={[300, 400, 500, 640, 800]}
              sizes="(max-width: 768px) 100vw, (max-width: 1024px) 50vw, 400px"
              class={`${previewImageClass} transition-opacity hover:opacity-95`}
            />
          </a>
        ) : (
          <Image
            src={project.previewImage}
            alt={project.previewAlt}
            widths={[300, 400, 500, 640, 800]}
            sizes="(max-width: 768px) 100vw, (max-width: 1024px) 50vw, 400px"
            class={previewImageClass}
          />
        )
      }
    </div>

    <div class="px-4 pb-5">
      <h3 class="text-xl font-semibold text-dark-moss">{project.name}</h3>

      <dl class="mt-4 space-y-3 text-base text-interior-designer dark:text-dim-text-muted">
        <div>
          <dt class="sr-only">Problem</dt>
          <dd class="text-[0.9rem] sm:text-base">
            <span class="font-medium text-dark-moss">Problem ·</span>{" "}
            {project.objective}
          </dd>
        </div>
        <div>
          <dt class="sr-only">Solution</dt>
          <dd class="text-[0.9rem] sm:text-base">
            <span class="font-medium text-dark-moss">Solution ·</span>{" "}
            {project.tradeOff}
          </dd>
        </div>
      </dl>

      <ul class="mt-4 flex flex-wrap gap-2" aria-label={`${project.name} tech stack`}>
        {
          project.stack.map((tag) => (
            <li class="rounded-full bg-skin/20 px-3 py-1 text-[0.7rem] sm:text-xs text-dark-moss dark:bg-dim-muted/20 dark:text-dark-moss">
              {tag}
            </li>
          ))
        }
      </ul>

      <div class="mt-6 flex flex-wrap gap-3">
        {
          project.demoUrl && (
            <a
              href={project.demoUrl}
              target="_blank"
              rel="noopener noreferrer"
              class="btn btn-soft btn-soft-default text-[0.7rem] sm:text-[0.9rem]"
            >
              Take a look ↗
            </a>
          )
        }
        {
          project.githubUrl && (
            <a
              href={project.githubUrl}
              target="_blank"
              rel="noopener noreferrer"
              class="btn btn-soft btn-github text-[0.7rem] sm:text-[0.9rem]"
            >
              GitHub ↗
            </a>
          )
        }
        <!-- <a
          href={`${import.meta.env.BASE_URL}projects/${project.slug}/`}
          class="btn btn-soft btn-soft-default text-[0.7rem] sm:text-[0.9rem]"
        >
          Read more...
        </a> -->
      </div>
    </div>
  </div>
</article>
